import axios from 'axios';

export const fetchPhones = async () => {
    let phones;

    try {
        phones = await axios.get('http://localhost:5000/api/phones');
        console.dir(phones);
    } catch (error) {
        console.error(error.message);
        phones = [];
    }
    
    return phones.data;
};

export const loadMorePhones = async () => {
    let phones;

    try {
        phones = await axios.get('http://localhost:5000/api/phones');
    } catch (error) {
        console.error(error.message);
        phones = [];
    }
    
    return phones.data;
};

export const fetchPhoneById = async id => {
    let phone;

    try {
        phone = await axios.get(`http://localhost:5000/api/phones/${id}`);
    } catch (error) {
        console.error(error.message);
        phone = {};
    }
    
    return phone.data;
};

export const fetchCategories = async () => {
    let categories;

    try {
        categories = await axios.get('http://localhost:5000/api/categories');
    } catch (error) {
        console.error(error.message);
        categories = [];
    }
    
    return categories.data;
};
