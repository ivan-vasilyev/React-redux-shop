import { SEARCH_PHONE } from 'actionTypes';

const initialState = '';

export default (state = initialState, { type, payload }) => {
    switch (type) {
        case SEARCH_PHONE:
            return payload;

        default:
            return state;
    }
}